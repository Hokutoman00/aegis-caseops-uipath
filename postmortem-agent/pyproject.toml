[project]
name = "aegis-caseops-postmortem"
version = "0.1.0"
description = "Aegis CaseOps deterministic postmortem formatter (O4 baseline) for UiPath AgentHack 2026"
authors = [{ name = "Hokuto Torigoe" }]
requires-python = ">=3.11"
dependencies = [
    "uipath>=2.10",
]
